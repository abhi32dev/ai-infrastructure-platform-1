# Project 15 — Optimized LLM inference

Runnable continuous-batching, admission-control, priority, KV-cache, prefix-cache, quantization-accounting, and speculative-decoding lab. See `PROD.md` for the test matrix and design reasoning.
