# Project 14 — PyTorch distributed training

Runnable CPU data-parallel training lab covering partitioning, all-reduce semantics, atomic checkpoints, worker failure, deterministic resume, framework detection, and production validation. See `PROD.md` for the test matrix and design reasoning.
